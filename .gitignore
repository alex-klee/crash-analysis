# Raw crash exports live in raw_data/ — too large for GitHub (100 MB/file limit),
# kept local only. Reproducible from the CT Crash Data Repository; each query's
# date range is recorded in raw_data/exportdescription_*.txt (which ARE tracked).
*.csv
raw_data/*.csv

# ...but DO keep the small compiled dataset and result tables.
# (Population is fetched live from the Census API, not stored.)
!crash_counts_by_town.csv
!output/
!output/*.csv
!output/*.png

# Tracked intentionally: crash-analysis.R, raw_data/exportdescription_*.txt, "Mark Instructions"
